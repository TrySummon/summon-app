pre-commit:
  commands:
    format:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx prettier --write {staged_files}
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx eslint --fix {staged_files}
    typecheck:
      glob: "*.{ts,tsx}"
      run: npm run typecheck
